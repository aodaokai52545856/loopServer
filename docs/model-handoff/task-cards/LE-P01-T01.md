# LE-P01-T01 — Bootstrap the Spring Boot control plane

**阶段：** 阶段 01：仓库骨架与控制面基础
**实施计划：** `../../实施计划-01-仓库骨架与控制面基础.md`
**计划任务标题：** `Bootstrap the Spring Boot control plane`
**前置任务：** 无

## 唯一目标
建立可启动、可用 PostgreSQL Testcontainer 加载上下文的 Spring Boot 控制面

## 明确不做
- 不实现源计划当前 Task 以外的 Step、文件或后续 Task。
- 不改变 `01-不可变架构决策.md`。

## 前置条件
- 前置任务状态为 `ACCEPTED`；“无”时不检查前置 Task。
- 启动提示词中的分支、Base commit 和工作区状态与实际一致。
- 源计划当前 Task 列出的前置文件存在；不存在时返回 `BLOCKED`。

## 开始状态
- 当前分支和 HEAD 必须与动态启动提示词完全一致；detached HEAD 时返回 `BLOCKED`。
- `git status --short` 只能包含启动提示词逐项声明的已知偏差；出现其他路径时返回 `BLOCKED`。
- 运行源 Task 要求的基线或 Red 命令前，不得修改文件、安装依赖或创建 commit。

## 必须阅读
- `../00-模型执行总则.md`
- `../01-不可变架构决策.md`
- `../../实施计划-01-仓库骨架与控制面基础.md` 中 `Bootstrap the Spring Boot control plane` 的完整内容
- `../../循环工程一期架构设计.md` 中 “3. 总体架构”“4. 开源组件选型”“13. 数据模型”“17. 测试策略”
- 当前 Task 白名单内已经存在的代码和测试

## 允许修改
- `pom.xml`
- `control-plane/pom.xml`
- `control-plane/src/main/java/com/company/loopengine/LoopEngineApplication.java`
- `control-plane/src/main/resources/application.yml`
- `control-plane/src/test/java/com/company/loopengine/LoopEngineApplicationTest.java`

## 禁止修改
- 上述白名单以外的所有文件。
- 已经提交的 Flyway migration、正式架构设计和其他任务卡。
- 当前 Task 未明确要求的依赖版本、接口、状态名和安全策略。

## 不可变约束
- 基础层不得接入真实 GitLab、真实节点或 OpenCode；跨语言数据只能通过 `contracts/v1`。
- 业务状态变更和 Outbox 必须同事务；个人节点不得获得仓库写权限。

## 执行顺序
1. 完成只读预检并等待“开始执行 Task LE-P01-T01”。
2. 严格执行源 Task 的 Step 1，写失败测试。
3. 执行 Step 2，失败原因必须与计划一致。
4. 执行 Step 3 的最小实现，不提前实现后续 Task。
5. 执行 Step 4 的全部命令和本卡验收命令。
6. 运行 `git diff --check`，核对白名单和敏感信息。
7. 仅在启动提示词允许时执行 Step 5 commit；随后停止。

## 验收命令
- `mvn -pl control-plane test -Dtest=LoopEngineApplicationTest`（预期成功；命令型步骤退出码为 0）

## 停止条件
- 适用 `00-模型执行总则.md` 的全部停止条件。
- 任一验收命令无法运行、退出码非预期或需要白名单外修改时立即返回 `BLOCKED`。

## 完成报告
使用 `../04-完成报告模板.md`；必须列出 Red、Green、模块验证、`git diff --check` 和 commit 证据。
