# LE-P04-T04 — Expose attempt bootstrap and ordered event APIs

**阶段：** 阶段 04：节点运行时与 OpenCode 执行器
**实施计划：** `../../实施计划-04-节点运行时与OpenCode执行器.md`
**计划任务标题：** `Expose attempt bootstrap and ordered event APIs`
**前置任务：** LE-P04-T03

## 唯一目标
控制面创建单 attempt、短期任务令牌、连续事件 ack 和附件代理

## 明确不做
- 不实现源计划当前 Task 以外的 Step、文件或后续 Task。
- 不改变 `01-不可变架构决策.md`。

## 前置条件
- 前置任务状态为 `ACCEPTED`；“无”时不检查前置 Task。
- 启动提示词中的分支、Base commit 和工作区状态与实际一致。
- 源计划当前 Task 列出的前置文件存在；不存在时返回 `BLOCKED`。

## 开始状态
- 当前分支和 HEAD 必须与动态启动提示词完全一致；detached HEAD 时返回 `BLOCKED`。
- `git status --short` 只能包含启动提示词逐项声明的已知偏差；出现其他路径时返回 `BLOCKED`。
- 运行源 Task 要求的基线或 Red 命令前，不得修改文件、安装依赖或创建 commit。

## 必须阅读
- `../00-模型执行总则.md`
- `../01-不可变架构决策.md`
- `../../实施计划-04-节点运行时与OpenCode执行器.md` 中 `Expose attempt bootstrap and ordered event APIs` 的完整内容
- `../../循环工程一期架构设计.md` 中 “8. 节点注册与治理”“9. 修复执行流程”“10. OpenCode 集成”“11. 代码发布与凭据”“15. 错误恢复”
- 当前 Task 白名单内已经存在的代码和测试

## 允许修改
- `control-plane/src/main/resources/db/migration/V4__execution.sql`
- `control-plane/src/main/java/com/company/loopengine/execution/AttemptBootstrapController.java`
- `control-plane/src/main/java/com/company/loopengine/execution/AttemptEventController.java`
- `control-plane/src/main/java/com/company/loopengine/execution/AttemptService.java`
- `control-plane/src/main/java/com/company/loopengine/execution/AttachmentProxyController.java`
- `control-plane/src/test/java/com/company/loopengine/execution/AttemptApiTest.java`

## 禁止修改
- 上述白名单以外的所有文件。
- 已经提交的 Flyway migration、正式架构设计和其他任务卡。
- 当前 Task 未明确要求的依赖版本、接口、状态名和安全策略。

## 不可变约束
- 节点只读 clone 并生成候选 Artifact；OpenCode 环境不得含 GitLab/任务凭据；事件 append-before-send；验证命令不经过 shell。
- 业务状态变更和 Outbox 必须同事务；个人节点不得获得仓库写权限。

## 执行顺序
1. 完成只读预检并等待“开始执行 Task LE-P04-T04”。
2. 严格执行源 Task 的 Step 1，写失败测试。
3. 执行 Step 2，失败原因必须与计划一致。
4. 执行 Step 3 的最小实现，不提前实现后续 Task。
5. 执行 Step 4 的全部命令和本卡验收命令。
6. 运行 `git diff --check`，核对白名单和敏感信息。
7. 仅在启动提示词允许时执行 Step 5 commit；随后停止。

## 验收命令
- `mvn -pl control-plane test -Dtest=AttemptApiTest`（预期成功；命令型步骤退出码为 0）

## 停止条件
- 适用 `00-模型执行总则.md` 的全部停止条件。
- 任一验收命令无法运行、退出码非预期或需要白名单外修改时立即返回 `BLOCKED`。

## 完成报告
使用 `../04-完成报告模板.md`；必须列出 Red、Green、模块验证、`git diff --check` 和 commit 证据。
